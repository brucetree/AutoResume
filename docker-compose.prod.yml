services:
  nginx:
    image: nginx:alpine
    container_name: autoresume-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - certbot-etc:/etc/letsencrypt:ro
      - certbot-www:/var/www/certbot:ro
    depends_on:
      - frontend
      - backend

  certbot:
    image: certbot/certbot
    container_name: autoresume-certbot
    restart: unless-stopped
    volumes:
      - certbot-etc:/etc/letsencrypt
      - certbot-lib:/var/lib/letsencrypt
      - certbot-www:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"

  backend:
    image: ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/autoresume-backend:latest
    container_name: autoresume-backend
    restart: unless-stopped
    expose:
      - "5001"
    env_file:
      - .env.backend
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:5001/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    image: ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/autoresume-frontend:latest
    container_name: autoresume-frontend
    restart: unless-stopped
    expose:
      - "3000"
    env_file:
      - .env.frontend
    environment:
      - INTERNAL_API_URL=http://backend:5001
    depends_on:
      backend:
        condition: service_healthy

volumes:
  certbot-etc:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/ec2-user/autoresume/certbot/conf
  certbot-lib:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/ec2-user/autoresume/certbot/lib
  certbot-www:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/ec2-user/autoresume/certbot/www
